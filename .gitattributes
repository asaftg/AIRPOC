# Shell scripts and other Unix-run text must stay LF — a CRLF shebang makes the kernel
# look for "/bin/bash\r" and the service dies with 203/EXEC "No such file or directory".
*.sh    text eol=lf
*.bash  text eol=lf
